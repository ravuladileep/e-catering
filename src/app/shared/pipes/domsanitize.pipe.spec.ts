import { DomsanitizePipe } from './domsanitize.pipe';

describe('DomsanitizePipe', () => {
  it('create an instance', () => {
    const pipe = new DomsanitizePipe();
    expect(pipe).toBeTruthy();
  });
});
